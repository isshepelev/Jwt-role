spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/authenticationspring
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create